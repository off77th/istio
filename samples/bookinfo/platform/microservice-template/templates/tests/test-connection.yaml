apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "microservice-template.fullname" . }}-test-connection"
  labels:
    {{- include "microservice-template.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "microservice-template.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
