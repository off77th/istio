apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookinfo-chart.fullname" . }}
  labels:
    {{- include "bookinfo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bookinfo-chart.selectorLabels" . | nindent 4 }}
